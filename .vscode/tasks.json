{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "ps2exe",
            "type": "shell",
            "command": "Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass; Invoke-ps2exe obsidian.ps1 bin\\obsidian.exe",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}